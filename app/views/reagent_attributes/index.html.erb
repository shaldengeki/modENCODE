<h1>Listing reagent_attributes</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Reagent type</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @reagent_attributes.each do |reagent_attribute| %>
  <tr>
    <td><%= reagent_attribute.name %></td>
    <td><%= reagent_attribute.reagent_type %></td>
    <td><%= link_to 'Show', reagent_attribute %></td>
    <td><%= link_to 'Edit', edit_reagent_attribute_path(reagent_attribute) %></td>
    <td><%= link_to 'Destroy', reagent_attribute, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Reagent attribute', new_reagent_attribute_path %>
