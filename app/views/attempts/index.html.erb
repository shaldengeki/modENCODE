<h1>Attempts</h1>

<table class="table table-bordered table-striped">
  <thead>
    <tr>
      <th>Pipeline</th>
      <th>Reagent</th>
      <th>Users</th>
      <th>Status</th>
      <th>Started</th>
      <th>Last updated</th>
    </tr>
  </thead>
  <tbody>
<% @attempts.each do |attempt| %>
    <tr>
      <td><%= attempt.pipeline.nil? ? "None" : link_to(attempt.pipeline.name, attempt) %></td>
      <td><%= attempt.reagent.nil? ? "None" : link_to(attempt.reagent.name, attempt.reagent) %></td>
      <td><%= attempt.users.nil? ? "None" : attempt.users.map(&:name).join(", ") %></td>
      <td><%= attempt.statuses.nil? ? "Planning" : attempt.statuses.order('updated_at DESC').first.description %></td>
      <td><%= I18n.localize(attempt.created_at, :format => :short) %></td>
      <td><%= I18n.localize(attempt.updated_at, :format => :short) %></td>
    </tr>
<% end %>
  </tbody>
</table>

<br />

<%= link_to 'New Attempt', new_attempt_path %>
