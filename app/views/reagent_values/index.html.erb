<h1>Listing reagent_values</h1>

<table>
  <tr>
    <th>Value</th>
    <th>Reagent attribute</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @reagent_values.each do |reagent_value| %>
  <tr>
    <td><%= reagent_value.value %></td>
    <td><%= reagent_value.reagent_attribute %></td>
    <td><%= link_to 'Show', reagent_value %></td>
    <td><%= link_to 'Edit', edit_reagent_value_path(reagent_value) %></td>
    <td><%= link_to 'Destroy', reagent_value, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Reagent value', new_reagent_value_path %>
