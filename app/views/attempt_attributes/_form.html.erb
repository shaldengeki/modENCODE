<%= form_for(@attempt_attribute) do |f| %>
  <% if @attempt_attribute.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@attempt_attribute.errors.count, "error") %> prohibited this attempt_attribute from being saved:</h2>

      <ul>
      <% @attempt_attribute.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :reagent %><br />
    <%= f.text_field :reagent %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
