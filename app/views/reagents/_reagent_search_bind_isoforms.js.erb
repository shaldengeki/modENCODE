$("input[name=isoforms_json]").change(function() {
  isoforms = $("input[name=isoforms_json]").val();
  if (isoforms.length > 2) {
    $.get("<%= search_reagents_by_isoforms_url(:model_type => model_type) %>", { isoforms: isoforms });
  }
});