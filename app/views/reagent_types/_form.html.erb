<%= form_for(@reagent_type) do |f| %>
  <% if @reagent_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@reagent_type.errors.count, "error") %> prohibited this reagent_type from being saved:</h2>

      <ul>
      <% @reagent_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
